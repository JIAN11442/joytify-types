export enum PasswordResetStatus {
  INITIAL = "initial",
  SUCCESS = "success",
  FAILED = "failed",
}

export enum ProfileCollections {
  PLAYLISTS = "playlists",
  SONGS = "songs",
  ALBUMS = "albums",
  FOLLOWING = "following",
}
